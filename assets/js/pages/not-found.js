/**
 * 404.
 *
 * Nothing to load — the shell is the whole page. It still runs so the
 * header, drawer, footer and theme behave the same as everywhere else.
 */

import { initShell } from '../core/shell.js';

initShell();
